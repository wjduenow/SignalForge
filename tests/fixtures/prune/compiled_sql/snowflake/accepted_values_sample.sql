WITH sample AS (SELECT * FROM "FAKE_PROJECT"."DATASET"."ORDERS" AS t WHERE MOD(ABS(HASH(*)), 10) < 1 LIMIT 100000) SELECT "STATUS" FROM sample WHERE "STATUS" IS NOT NULL AND "STATUS" NOT IN ('placed', 'shipped', 'cancelled')